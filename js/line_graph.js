// -------------------------------------------------------------------------- //
//                              LINE GRAPHS                                   //
// -------------------------------------------------------------------------- //

function fancyLine (svg) {

	var rect = svg.getBoundingClientRect(); // get the bounding rectangle

	width = rect.width;
	height = rect.height;

	// console.log( rect.width  );
	// console.log( rect.height );
}
